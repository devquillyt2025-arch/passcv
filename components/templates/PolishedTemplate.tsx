import makeTemplate from './baseRenderer';

// Accent header band with clean hairline-rule headings. Visual variety.
export default makeTemplate({
  accent: '#0891b2',
  fontFamily: 'Helvetica',
  header: 'band',
  heading: 'rule',
  nameSize: 24,
  skills: 'inline',
});
